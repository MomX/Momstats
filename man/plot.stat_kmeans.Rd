% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_kmeans.R
\name{plot.stat_kmeans}
\alias{plot.stat_kmeans}
\title{Plot k-means clustering results}
\usage{
\method{plot}{stat_kmeans}(x, type = c("clusters", "withinss"), color = NULL, labels = NULL, ...)
}
\arguments{
\item{x}{A \code{stat_kmeans} object}

\item{type}{Character. Type of plot:
\itemize{
\item \code{"clusters"}: Cluster visualization (requires PCA projection)
\item \code{"withinss"}: Within-cluster sum of squares by cluster
}}

\item{color}{Column name (bare or quoted) for coloring points (for cluster plot).
If NULL, colors by cluster assignment.}

\item{labels}{Column name (bare or quoted) for text labels.}

\item{...}{Additional arguments (reserved)}
}
\value{
NULL (invisibly). Draws plot as side effect.
}
\description{
Visualize k-means clustering with various plot types.
}
\examples{
\dontrun{
km <- boteft \%>\% stat_kmeans(k = 3)

# Cluster plot (colored by cluster)
plot(km)

# Color by original grouping
plot(km, color = type)

# Within-cluster SS plot
plot(km, type = "withinss")
}

}
