% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_lm.R
\name{collect.stat_lm}
\alias{collect.stat_lm}
\title{Collect fitted values from linear model}
\usage{
\method{collect}{stat_lm}(x, data = NULL, fold = FALSE, residuals = FALSE, ...)
}
\arguments{
\item{x}{A \code{stat_lm} object}

\item{data}{A tibble. If NULL, uses the original data from the model.}

\item{fold}{How to add fitted values:
\itemize{
\item \code{FALSE} (default): Add as columns matching response structure
\item \code{TRUE}: Fold into list-column (for coe responses)
\item Character: Name for folded column
}}

\item{residuals}{Logical. Should residuals be included? Default \code{FALSE}.}

\item{...}{Additional arguments (reserved)}
}
\value{
A tibble with fitted values (and optionally residuals) added
}
\description{
Extract fitted values and residuals from a linear model and add them to a tibble.
}
\examples{
\dontrun{
lm_fit <- boteft \%>\% stat_lm(coe ~ length)

# Add fitted values
collect(lm_fit)

# With residuals
collect(lm_fit, residuals = TRUE)

# Fold into list-column
collect(lm_fit, fold = TRUE)
}

}
