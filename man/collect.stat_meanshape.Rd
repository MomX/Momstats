% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_meanshapes.R
\name{collect.stat_meanshape}
\alias{collect.stat_meanshape}
\title{Collect mean shapes and add to original data}
\usage{
\method{collect}{stat_meanshape}(x, data = NULL, prefix = "mean_", ...)
}
\arguments{
\item{x}{A \code{stat_meanshape} object}

\item{data}{A tibble. If NULL, uses the original data from the analysis.}

\item{prefix}{Character. Prefix for mean shape columns. Default "mean_".}

\item{...}{Additional arguments (reserved)}
}
\value{
A tibble with mean shape columns added (and their \verb{_i} inverses)
}
\description{
Add mean shapes back to the original data, matched by group.
}
\details{
For grouped mean shapes, mean shapes are matched back to original rows
by group membership. For global mean shapes, the same mean is added to
all rows.

New columns added:
\itemize{
\item \verb{\{prefix\}\{coe_col\}}: Mean coefficient column (e.g., \code{mean_VD})
\item \verb{\{prefix\}\{coe_col\}_i}: Mean shape (e.g., \code{mean_VD_i})
}
}
\examples{
\dontrun{
ms <- vitis \%>\% stat_meanshape(status)

# Add mean shapes to data
vitis_with_means <- collect(ms)

# Custom prefix
vitis_with_means <- collect(ms, prefix = "grp_mean_")
}

}
