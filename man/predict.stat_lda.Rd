% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_lda.R
\name{predict.stat_lda}
\alias{predict.stat_lda}
\title{Predict method for LDA}
\usage{
\method{predict}{stat_lda}(object, newdata, retain = FALSE, fold = FALSE, .collect = TRUE, ...)
}
\arguments{
\item{object}{A \code{stat_lda} object}

\item{newdata}{A tibble with the same predictor columns as training data}

\item{retain}{How many LDs to return:
\itemize{
\item \code{FALSE} (default): No LD scores, only predictions
\item \code{TRUE}: All LDs
\item Integer (e.g., \code{2}): First N LDs
\item Numeric 0-1 (e.g., \code{0.95}): LDs explaining this proportion of variance
}}

\item{fold}{How to return LD scores (only if retain != FALSE):
\itemize{
\item \code{FALSE} (default): Add as separate columns (\code{LD1}, \code{LD2}, ...)
\item \code{TRUE}: Fold into single list-column named \code{"lda"}
\item Character: Fold into single list-column with this name
}}

\item{.collect}{Logical. Should predictions be added to \code{newdata} (TRUE, default)
or returned as a standalone tibble (FALSE)?}

\item{...}{Additional arguments (reserved)}
}
\value{
If \code{.collect = TRUE}, returns \code{newdata} with predictions added. If
\code{.collect = FALSE}, returns a tibble with identifier columns (non-predictors) and
predictions only. Always includes:
\itemize{
\item \code{pred}: Predicted class (factor)
\item \code{prob}: Posterior probability of predicted class (numeric)
\item \code{LD1}, \code{LD2}, ... : LD scores (if \code{retain} is not FALSE)
}
}
\description{
Predict class membership and discriminant scores for new data using a fitted LDA model.
}
\examples{
\dontrun{
# Train LDA
lda <- boteft \%>\% stat_lda(type)

# Predict on new data
new_preds <- predict(lda, new_data)

# Pipe the model
new_preds <- training \%>\%
  stat_lda(type) \%>\%
  predict(testing)
}

}
