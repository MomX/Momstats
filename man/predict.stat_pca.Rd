% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_pca.R
\name{predict.stat_pca}
\alias{predict.stat_pca}
\title{Predict method for PCA}
\usage{
\method{predict}{stat_pca}(object, newdata, retain = NULL, fold = FALSE, .collect = TRUE, ...)
}
\arguments{
\item{object}{A \code{stat_pca} object}

\item{newdata}{A tibble with the same predictor columns as training data}

\item{retain}{How many PCs to return:
\itemize{
\item \code{NULL} (default): All PCs
\item Integer (e.g., \code{5}): First N PCs
\item Numeric 0-1 (e.g., \code{0.95}): PCs explaining this proportion of variance
}}

\item{fold}{How to return PC scores:
\itemize{
\item \code{FALSE} (default): Add as separate columns (\code{PC1}, \code{PC2}, ...)
\item \code{TRUE}: Fold into single list-column named \code{"pca"}
\item Character: Fold into single list-column with this name
}}

\item{.collect}{Logical. Should predictions be added to \code{newdata} (TRUE, default)
or returned as a standalone tibble (FALSE)?}

\item{...}{Additional arguments (reserved)}
}
\value{
If \code{.collect = TRUE}, returns \code{newdata} with PC scores added. If
\code{.collect = FALSE}, returns a tibble with identifier columns (non-coe) and
PC scores only.
}
\description{
Project new data onto principal components from a fitted PCA model.
}
\examples{
\dontrun{
# Train PCA
pca <- boteft \%>\% stat_pca()

# Predict on new data
new_scores <- predict(pca, new_data)

# Pipe the model
new_scores <- training \%>\%
  stat_pca() \%>\%
  predict(testing)
}

}
