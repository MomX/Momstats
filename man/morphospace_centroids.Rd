% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morphospaces.R
\name{morphospace_centroids}
\alias{morphospace_centroids}
\title{Morphospace visualization: group centroids}
\usage{
morphospace_centroids(
  object,
  group = NULL,
  group_vals = NULL,
  transduce = TRUE,
  template = TRUE,
  draw = TRUE,
  ...
)
}
\arguments{
\item{object}{A stat_* object (stat_pca, stat_lda, etc.)}

\item{group}{Column name for grouping (bare or quoted). For PCA, required.
For LDA, optional (uses discriminant groups if NULL).}

\item{group_vals}{Pre-evaluated group values (used internally by plot).}

\item{transduce}{Logical. Should shapes be transduced? Default TRUE.}

\item{template}{Logical or numeric. Templating option. Default TRUE.}

\item{draw}{Logical. Should shapes be drawn? Default TRUE.}

\item{...}{Additional graphical parameters (lwd, col, etc.)}
}
\value{
A tibble with positions, coefficients, and shapes
}
\description{
Create shapes at group centroid positions.
}
\examples{
\dontrun{
pca <- boteft \%>\% stat_pca()
plot(pca, color = type, morphospace = "centroids")

# Or call directly
plot(pca, morphospace = FALSE)
morphospace_centroids(pca, group = type)
}

}
