% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_lda.R
\name{plot.stat_lda}
\alias{plot.stat_lda}
\title{Plot LDA results}
\usage{
\method{plot}{stat_lda}(
  x,
  type = c("scores", "loadings"),
  lds = c(1, 2),
  color = NULL,
  labels = NULL,
  chull = TRUE,
  cex = 0.7,
  legend = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{stat_lda} object}

\item{type}{Character. Type of plot:
\itemize{
\item \code{"scores"}: LD score plot (default) - scatter for 2+ LDs, boxplot for 1 LD
\item \code{"loadings"}: Loading plot for first two LDs
}}

\item{lds}{Integer vector of length 2. Which LDs to plot for score/loading plots.
Default is \code{c(1, 2)}. For binary classification (1 LD), only first element used.}

\item{color}{Column name (bare or quoted) for coloring points. Default uses
the response variable.}

\item{labels}{Column name (bare or quoted) for text labels in score plot.
For loadings, logical: should variable names be shown? Default \code{TRUE}.}

\item{chull}{Logical. Draw convex hulls around groups? Only works when \code{color}
is a factor. Default \code{TRUE}.}

\item{cex}{Numeric. Character expansion for labels. Default is 0.7.}

\item{legend}{Logical. Show legend for colors? Default \code{TRUE}.}

\item{...}{Additional arguments (reserved for future use)}
}
\value{
NULL (invisibly). Draws plot as side effect.
}
\description{
Visualize LDA results with discrimination plots, loading plots, or histograms
(for binary classification).
}
\examples{
\dontrun{
lda <- boteft \%>\% stat_lda(type)

# Score plot (default)
plot(lda)
plot(lda, color = type)
plot(lda, labels = pred)
plot(lda, color = type, chull = TRUE)

# Different LDs
plot(lda, lds = c(2, 3))

# Loading plot
plot(lda, type = "loadings")
plot(lda, type = "loadings", labels = FALSE)
}

}
