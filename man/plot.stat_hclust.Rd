% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_hclust.R
\name{plot.stat_hclust}
\alias{plot.stat_hclust}
\title{Plot hierarchical clustering tree}
\usage{
\method{plot}{stat_hclust}(
  x,
  type = c("unrooted", "dendrogram", "fan", "phylogram"),
  color = NULL,
  tip_labels = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{stat_hclust} object}

\item{type}{Character. Type of plot:
\itemize{
\item \code{"unrooted"}: Unrooted phylogram (default, requires ape)
\item \code{"dendrogram"}: Classic dendrogram
\item \code{"fan"}: Fan/circular tree (requires ape)
\item \code{"phylogram"}: Rectangular phylogram (requires ape)
}}

\item{color}{Column name (bare or quoted) for coloring tips. If NULL and tree
was cut, colors by cluster assignment.}

\item{tip_labels}{Logical. Show tip labels? Default TRUE for small trees (<50 tips).}

\item{...}{Additional arguments passed to plotting functions}
}
\value{
NULL (invisibly). Draws plot as side effect.
}
\description{
Visualize hierarchical clustering with various tree layouts.
}
\details{
Most plot types require the \code{ape} package. If not installed, falls back to
base dendrogram plot.
}
\examples{
\dontrun{
hc <- boteft \%>\% stat_hclust(k = 4)

# Unrooted tree (default)
plot(hc)

# Color by original grouping
plot(hc, color = type)

# Color by clusters
plot(hc)  # Automatic if tree was cut

# Classic dendrogram
plot(hc, type = "dendrogram")

# Fan tree
plot(hc, type = "fan")
}

}
