% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_meanshapes.R
\name{stat_meanshape}
\alias{stat_meanshape}
\title{Calculate mean (or aggregated) shapes}
\usage{
stat_meanshape(data, formula = NULL, .fn = mean, ...)
}
\arguments{
\item{data}{A tibble with coefficient columns}

\item{formula}{A formula or bare column name specifying grouping:
\itemize{
\item Missing/NULL: Global mean across all observations
\item Bare name: \code{status} → group by this column, use all coe columns
\item Formula: \code{status ~ .} → explicit all coe columns
\item Formula: \code{status ~ VD} → specific coe column(s)
\item Formula: \code{~ status} → one-sided formula (group by status)
}}

\item{.fn}{Aggregation function. Default \code{mean}. Can be \code{median} or any
function that takes a matrix column and returns a scalar.}

\item{...}{Additional arguments (reserved for future use)}
}
\value{
An object of class \code{c("stat_meanshape", "momstats")} containing:
\itemize{
\item \code{data}: Original tibble (unchanged)
\item \code{summary}: Tibble with group(s), n, and mean coe column(s)
\item \code{method}: "meanshape"
\item \code{call}: The function call
\item \code{formula}: Formula used (if any)
\item \code{group_cols}: Grouping column name(s) (NULL if global)
\item \code{coe_cols}: Coefficient column names
\item \code{.fn}: Aggregation function name
}
}
\description{
Compute mean or other aggregated shapes, either globally or by groups.
}
\details{
\code{stat_meanshape()} provides a unified interface for aggregating shapes.
It handles:
\itemize{
\item \strong{Global aggregation}: No grouping, single mean shape
\item \strong{Grouped aggregation}: By one or more grouping variables
\item \strong{Flexible syntax}: Bare names or formula syntax
\item \strong{Multiple coe columns}: Aggregates all coe columns simultaneously
\item \strong{Custom aggregation}: mean (default), median, or custom function
\item \strong{Automatic inverse}: Computes reconstructed shapes via \code{coe_class_i()}
}
\subsection{Formula syntax}{

The formula specifies grouping and optionally which coe columns:
\itemize{
\item Missing: Global mean of all coe columns
\item \code{status}: Group by status, all coe columns
\item \code{~ status}: Same as above
\item \code{status ~ .}: Explicit all coe columns
\item \code{status ~ VD}: Only VD coe column
\item \code{status ~ VD + VL}: Multiple specific coe columns
}
}

\subsection{Aggregation functions}{
\itemize{
\item \code{.fn = mean} (default): Arithmetic mean
\item \code{.fn = median}: Median shape
\item Custom: Any function taking a numeric vector and returning a scalar
}
}

\subsection{Getting results}{

Use \code{\link[=collect]{collect()}} to add mean shapes back to original data (matched by group):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{ms <- vitis \%>\% stat_meanshape(status)
vitis_with_means <- collect(ms)  # Adds mean_VD, mean_VL, etc.
}\if{html}{\out{</div>}}
}
}
\examples{
\dontrun{
# Global mean
ms1 <- vitis \%>\% stat_meanshape()

# By group
ms2 <- vitis \%>\% stat_meanshape(status)

# Formula syntax
ms3 <- vitis \%>\% stat_meanshape(~ status)
ms4 <- vitis \%>\% stat_meanshape(status ~ .)

# Specific coe column
ms5 <- vitis \%>\% stat_meanshape(status ~ VD)

# Median instead of mean
ms6 <- vitis \%>\% stat_meanshape(status, .fn = median)

# Add mean shapes to data
vitis_means <- collect(ms2)

# Plot mean shapes
plot(ms2)  # Mean shapes only
plot(ms2, with_data = TRUE)  # Mean shapes + contributing data
}

}
\seealso{
\code{\link[=collect.stat_meanshape]{collect.stat_meanshape()}}, \code{\link[=plot.stat_meanshape]{plot.stat_meanshape()}}
}
